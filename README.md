# できた！トラッカー — 返信 & 欲求の我慢

「返信できた」「我慢できた」ボタンを押すたびに累計回数を記録し、
**1日ごとの回数を折れ線グラフで可視化**する、単体で動くWebアプリです。

## 特徴

- 2種類のボタン（返信できた / 欲求を我慢できた）を押すと、それぞれ累計に +1（押した達成感の演出つき）
- 各カードに 累計 と 今日 の回数を表示
- 1日ごとの回数を **折れ線グラフ**で可視化。返信（青）と我慢（緑）を **同じグラフに重ねて**表示（7日 / 14日 / 30日で切替）
- それぞれ直前の1回を取り消し
- **タスク（ToDo）機能**：返信が必要なタスクを登録し、チェックで完了すると **返信の回数も +1（相乗）** してグラフに反映。
  完了したタスクは「完了済み」に移動（取り消しも可能）。件数表示は「残り」を使わず、淡々と **「返信が必要な回数　◯件」** とグレーで表示
- データの書き出し・読み込み（バックアップ／端末移行用）
- 記録はブラウザの `localStorage` に自動保存。**開き直してもリセットされません**（サーバー送信なし）
- 外部ライブラリなし・`index.html` 単体で完結

## 使い方

- **そのまま使う**: `index.html` をダウンロードしてブラウザで開くだけ。
- **Webで公開する**: このリポジトリの Settings → Pages → Branch を `main` / `/ (root)` にして保存すると、
  `https://soulound.github.io/reply-tracker/` で公開されます。

## データについて

記録は開いた端末・ブラウザごとに保存されます。別の端末へ移すときは「データを書き出す」でJSONを保存し、
移行先で「読み込む」を使ってください。
